name: nested_task_strategy
description: "Nested: Task Strategy - Infinite nested tasks with weighted progress (scale 1-10)"
publish_to: 'none'
version: 2.1.4+7

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  uuid: ^4.3.3
  shared_preferences: ^2.2.2
  provider: ^6.1.1
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
